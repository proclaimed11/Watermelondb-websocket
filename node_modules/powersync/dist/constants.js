import { homedir } from 'node:os';
import path from 'node:path';
export const APP_NAME = 'POWERSYNC_CLI';
export const POWERSYNC_DIRECTORY = path.join(homedir(), '/.powersync');
export const ENV_FILE_PATH = `${POWERSYNC_DIRECTORY}/.env.powersync`;
export const ENDPOINTS = {
    ACCOUNTS_URL: 'https://accounts.journeyapps.com',
    POWERSYNC_URL: 'https://powersync-api.journeyapps.com'
};
export const LANGUAGES = ['js', 'dart'];
